#include <stdio.h>

int main(){
	int t,i;
	scanf("%d", &t);
	if(t>=1&&t<=20){
	for(i=0;i<t;i++){
	    int n,m;
	    scanf("%d %d",&n,&m);
	    if((n>=1&&n<=100)&&(m>=1&&m<=100000))
	    {
	        if(m%n==0)
	            printf("Yes\n");
	        else
	            printf("No\n");
	    }
	}}
}
