#include<stdio.h>

int main(){
    int l,r,k,i,count=0;
    scanf("%d %d %d",&l, &r, &k);
    if((l&&r&&k)>=1&&(l&&r&&k)<=1000){
        for(i=l;i<=r;i++){
            if(i%k==0)
                count++;
        }
        printf("%d",count);
    }
}
